package frc.robot.commands;

import edu.wpi.first.wpilibj.Joystick;
import edu.wpi.first.wpilibj2.command.ParallelDeadlineGroup;
import frc.robot.subsystems.DriveTrain;
//import frc.robot.commands.TankDrive;
//import frc.robot.commands.PickupPC;
import frc.robot.subsystems.Indexing;
import frc.robot.subsystems.Intake;

/*
public class pickupNdrive extends ParallelDeadlineGroup {
    public pickupNdrive(DriveTrain drive, Indexing index, Intake intake, Joystick leftJoy, Joystick rightJoy) {
        super(new PickupPC(index, intake), new TankDrive( () -> leftJoy.getY(), () -> rightJoy.getY(), drive ) );
            
    }

}
*/
